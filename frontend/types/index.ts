import type { AIResult, ReportPayload, ReportResponse, ReportStep, StepMeta, UrgencyLevel } from "./report";

export type { AIResult, ReportPayload, ReportResponse, ReportStep, StepMeta, UrgencyLevel };

export type UrgencyMeta = {
  label: string;
  color: string;
  bg: string;
};
